<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Graphos Services - Design & AI Solutions</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #0a0a0a;
            --secondary: #d4af37;
            --accent: #f8f9fa;
            --text: #2d3436;
            --light: #ffffff;
            --dark: #1a1a1a;
            --gradient: linear-gradient(135deg, #0a0a0a 0%, #2d2d2d 100%);
            --gold-gradient: linear-gradient(135deg, #d4af37 0%, #f9e076 50%, #d4af37 100%);
            --gold-light: rgba(212, 175, 55, 0.1);
            --shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            --transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        [dir="rtl"] {
            --text-align: right;
            --float: right;
        }

        [dir="ltr"] {
            --text-align: left;
            --float: left;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background: var(--accent);
            color: var(--text);
            line-height: 1.6;
            overflow-x: hidden;
            text-align: var(--text-align);
        }

        /* تصميم رأس الصفحة مع أنيميشن */
        header {
            background: var(--gradient);
            color: var(--light);
            padding: 1rem 0;
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
            box-shadow: var(--shadow);
            backdrop-filter: blur(10px);
            transition: var(--transition);
        }

        .header-scrolled {
            padding: 0.7rem 0;
            background: rgba(10, 10, 10, 0.95);
        }

        .nav-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .logo {
            font-size: 1.8rem;
            font-weight: bold;
            background: var(--gold-gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            animation: glow 2s ease-in-out infinite alternate;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .logo i {
            font-size: 2rem;
        }

        @keyframes glow {
            0% { text-shadow: 0 0 10px rgba(212, 175, 55, 0.5); }
            100% { text-shadow: 0 0 20px rgba(212, 175, 55, 0.8), 0 0 30px rgba(212, 175, 55, 0.6); }
        }

        nav ul {
            display: flex;
            list-style: none;
        }

        nav ul li {
            margin: 0 1rem;
            position: relative;
        }

        nav ul li a {
            color: var(--light);
            text-decoration: none;
            transition: var(--transition);
            font-weight: 500;
            padding: 0.5rem 0;
            position: relative;
        }

        nav ul li a::after {
            content: '';
            position: absolute;
            bottom: 0;
            right: 0;
            width: 0;
            height: 2px;
            background: var(--secondary);
            transition: var(--transition);
        }

        nav ul li a:hover::after {
            width: 100%;
        }

        nav ul li a:hover {
            color: var(--secondary);
        }

        .language-switcher {
            background: transparent;
            border: 1px solid var(--secondary);
            color: var(--secondary);
            padding: 0.5rem 1rem;
            border-radius: 20px;
            cursor: pointer;
            transition: var(--transition);
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .language-switcher:hover {
            background: var(--secondary);
            color: var(--primary);
            transform: translateY(-2px);
        }

        /* قسم البطل مع تأثيرات متقدمة */
        .hero {
            background: var(--gradient);
            color: var(--light);
            padding: 12rem 0 6rem;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1000 100" fill="%23d4af3710"><path d="M500 50Q400 0 300 50T100 50T0 0v100h1000V0Q900 50 800 50T600 50T500 50z"/></svg>') bottom center no-repeat;
            background-size: cover;
            animation: wave 20s linear infinite;
        }

        @keyframes wave {
            0% { transform: translateX(0); }
            50% { transform: translateX(-20px); }
            100% { transform: translateX(0); }
        }

        .hero-content {
            position: relative;
            z-index: 2;
            max-width: 800px;
            margin: 0 auto;
        }

        .hero h1 {
            font-size: 3.5rem;
            margin-bottom: 1.5rem;
            animation: fadeInUp 1s ease-out;
        }

        .hero p {
            font-size: 1.2rem;
            margin: 0 auto 2.5rem;
            animation: fadeInUp 1s ease-out 0.3s both;
            max-width: 700px;
            line-height: 1.8;
        }

        .ai-badge {
            display: inline-block;
            background: var(--gold-gradient);
            color: var(--primary);
            padding: 0.5rem 1.5rem;
            border-radius: 30px;
            font-weight: bold;
            margin-bottom: 2rem;
            animation: pulse 2s infinite;
            box-shadow: 0 5px 15px rgba(212, 175, 55, 0.3);
        }

        @keyframes pulse {
            0% { transform: scale(1); box-shadow: 0 5px 15px rgba(212, 175, 55, 0.3); }
            50% { transform: scale(1.05); box-shadow: 0 8px 25px rgba(212, 175, 55, 0.5); }
            100% { transform: scale(1); box-shadow: 0 5px 15px rgba(212, 175, 55, 0.3); }
        }

        .cta-button {
            display: inline-block;
            background: var(--gold-gradient);
            color: var(--primary);
            padding: 1rem 2.5rem;
            border-radius: 30px;
            text-decoration: none;
            font-weight: bold;
            transition: var(--transition);
            animation: fadeInUp 1s ease-out 0.6s both;
            box-shadow: 0 10px 20px rgba(212, 175, 55, 0.3);
            position: relative;
            overflow: hidden;
        }

        .cta-button::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
            transition: 0.5s;
        }

        .cta-button:hover::before {
            left: 100%;
        }

        .cta-button:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 30px rgba(212, 175, 55, 0.4);
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* قسم الخدمات */
        .services {
            padding: 6rem 0;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .section-title {
            text-align: center;
            margin-bottom: 4rem;
            position: relative;
        }

        .section-title h2 {
            font-size: 2.5rem;
            color: var(--primary);
            display: inline-block;
            position: relative;
        }

        .section-title h2::after {
            content: '';
            display: block;
            width: 80px;
            height: 4px;
            background: var(--gold-gradient);
            margin: 0.8rem auto;
            border-radius: 2px;
            animation: expand 1.5s ease-out;
        }

        @keyframes expand {
            from { width: 0; }
            to { width: 80px; }
        }

        .services-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 2.5rem;
        }

        .service-card {
            background: var(--light);
            border-radius: 15px;
            overflow: hidden;
            box-shadow: var(--shadow);
            transition: var(--transition);
            position: relative;
            opacity: 0;
            transform: translateY(30px);
        }

        .service-card.visible {
            opacity: 1;
            transform: translateY(0);
        }

        .service-card:hover {
            transform: translateY(-15px);
            box-shadow: 0 20px 40px rgba(0,0,0,0.15);
        }

        .service-icon {
            height: 140px;
            background: var(--gradient);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 3.5rem;
            color: var(--secondary);
            position: relative;
            overflow: hidden;
        }

        .service-icon::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--gold-light);
            opacity: 0.2;
        }

        .service-content {
            padding: 2rem;
        }

        .service-content h3 {
            font-size: 1.5rem;
            margin-bottom: 1rem;
            color: var(--primary);
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .service-content h3 i {
            color: var(--secondary);
        }

        .pricing-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 1.5rem;
        }

        .pricing-table th, .pricing-table td {
            padding: 1rem;
            text-align: var(--text-align);
            border-bottom: 1px solid #eee;
        }

        .pricing-table th {
            background: var(--gold-light);
            color: var(--primary);
            font-weight: 600;
        }

        .pricing-table tr:nth-child(even) {
            background: #f9f9f9;
        }

        .price {
            font-weight: bold;
            color: var(--secondary);
        }

        .negotiable {
            font-size: 0.85rem;
            color: #666;
            font-style: italic;
            margin-top: 1rem;
            display: block;
        }

        .add-to-cart {
            background: var(--primary);
            color: var(--light);
            border: none;
            padding: 0.6rem 1.2rem;
            border-radius: 4px;
            cursor: pointer;
            transition: var(--transition);
            display: flex;
            align-items: center;
            gap: 5px;
        }

        .add-to-cart:hover {
            background: var(--secondary);
            transform: translateY(-2px);
        }

        /* قسم بوت الذكاء الاصطناعي */
        .ai-assistant {
            position: fixed;
            bottom: 30px;
            left: 30px;
            z-index: 1000;
        }

        .ai-toggle {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            background: var(--gold-gradient);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--primary);
            font-size: 1.5rem;
            cursor: pointer;
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
            transition: var(--transition);
            animation: float 3s ease-in-out infinite;
        }

        @keyframes float {
            0% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
            100% { transform: translateY(0); }
        }

        .ai-toggle:hover {
            transform: scale(1.1);
            box-shadow: 0 8px 25px rgba(0,0,0,0.3);
        }

        .ai-chatbot {
            position: absolute;
            bottom: 80px;
            left: 0;
            width: 350px;
            background: var(--light);
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            display: none;
            flex-direction: column;
            overflow: hidden;
            animation: slideUp 0.3s ease-out;
        }

        @keyframes slideUp {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .ai-chatbot.active {
            display: flex;
        }

        .chat-header {
            background: var(--gradient);
            color: var(--light);
            padding: 1rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .chat-header h3 {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .close-chat {
            background: transparent;
            border: none;
            color: var(--light);
            font-size: 1.2rem;
            cursor: pointer;
        }

        .chat-messages {
            padding: 1rem;
            height: 300px;
            overflow-y: auto;
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .message {
            padding: 10px 15px;
            border-radius: 18px;
            max-width: 80%;
            animation: messageAppear 0.3s ease-out;
        }

        @keyframes messageAppear {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .bot-message {
            background: var(--gold-light);
            align-self: flex-start;
            border-bottom-right-radius: 5px;
        }

        .user-message {
            background: var(--gradient);
            color: var(--light);
            align-self: flex-end;
            border-bottom-left-radius: 5px;
        }

        .chat-input {
            display: flex;
            padding: 1rem;
            border-top: 1px solid #eee;
        }

        .chat-input input {
            flex: 1;
            padding: 0.8rem;
            border: 1px solid #ddd;
            border-radius: 20px;
            outline: none;
            transition: var(--transition);
        }

        .chat-input input:focus {
            border-color: var(--secondary);
        }

        .send-button {
            background: var(--secondary);
            color: var(--primary);
            border: none;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            margin-left: 10px;
            cursor: pointer;
            transition: var(--transition);
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .send-button:hover {
            background: var(--primary);
            color: var(--secondary);
            transform: rotate(15deg);
        }

        /* قسم السلة */
        .cart-section {
            padding: 6rem 0;
            background: #f5f5f5;
        }

        .cart-container {
            background: var(--light);
            border-radius: 15px;
            padding: 2.5rem;
            box-shadow: var(--shadow);
        }

        .cart-items {
            margin-bottom: 2rem;
        }

        .cart-item {
            display: flex;
            justify-content: space-between;
            padding: 1.2rem 0;
            border-bottom: 1px solid #eee;
            align-items: center;
            animation: fadeIn 0.5s ease-out;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        .cart-item-details {
            display: flex;
            flex-direction: column;
        }

        .cart-item-name {
            font-weight: 600;
            margin-bottom: 0.3rem;
        }

        .cart-item-desc {
            font-size: 0.9rem;
            color: #666;
        }

        .cart-item-price {
            font-weight: bold;
            color: var(--secondary);
        }

        .remove-item {
            background: transparent;
            border: none;
            color: #ff6b6b;
            cursor: pointer;
            font-size: 1.2rem;
            transition: var(--transition);
        }

        .remove-item:hover {
            color: #ff5252;
            transform: scale(1.2);
        }

        .cart-total {
            text-align: var(--text-align);
            font-size: 1.5rem;
            font-weight: bold;
            margin-bottom: 2rem;
            padding-top: 1rem;
            border-top: 2px solid #eee;
        }

        .checkout-button {
            background: var(--gold-gradient);
            color: var(--primary);
            border: none;
            padding: 1.2rem 2.5rem;
            border-radius: 30px;
            font-weight: bold;
            cursor: pointer;
            transition: var(--transition);
            display: block;
            width: 100%;
            font-size: 1.1rem;
            box-shadow: 0 10px 20px rgba(212, 175, 55, 0.3);
        }

        .checkout-button:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 30px rgba(212, 175, 55, 0.4);
        }

        .payment-policy {
            margin-top: 2rem;
            padding: 1.5rem;
            background: #f9f9f9;
            border-radius: 8px;
            border-right: 4px solid var(--secondary);
        }

        /* تذييل الصفحة */
        footer {
            background: var(--gradient);
            color: var(--light);
            padding: 4rem 0 2rem;
        }

        .footer-content {
            display: flex;
            justify-content: space-between;
            flex-wrap: wrap;
        }

        .footer-section {
            flex: 1;
            min-width: 300px;
            margin-bottom: 2rem;
        }

        .footer-section h3 {
            margin-bottom: 1.5rem;
            position: relative;
            display: inline-block;
        }

        .footer-section h3::after {
            content: '';
            display: block;
            width: 40px;
            height: 3px;
            background: var(--secondary);
            margin-top: 0.5rem;
        }

        .social-links {
            display: flex;
            gap: 15px;
            margin-top: 1.5rem;
        }

        .social-links a {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 40px;
            height: 40px;
            background: rgba(255,255,255,0.1);
            border-radius: 50%;
            color: var(--light);
            transition: var(--transition);
        }

        .social-links a:hover {
            background: var(--secondary);
            color: var(--primary);
            transform: translateY(-5px);
        }

        .copyright {
            margin-top: 3rem;
            padding-top: 1.5rem;
            border-top: 1px solid rgba(255,255,255,0.1);
            text-align: center;
        }

        /* تصميم متجاوب */
        @media (max-width: 768px) {
            .nav-container {
                flex-direction: column;
            }

            nav ul {
                margin-top: 1rem;
                flex-wrap: wrap;
                justify-content: center;
            }

            nav ul li {
                margin: 0.3rem 0.5rem;
            }

            .hero h1 {
                font-size: 2.5rem;
            }

            .services-grid {
                grid-template-columns: 1fr;
            }

            .ai-chatbot {
                width: 300px;
                left: -120px;
            }

            .language-switcher {
                margin-top: 1rem;
            }
        }

        /* تأثيرات إضافية */
        .floating-elements {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            overflow: hidden;
            z-index: 1;
        }

        .floating-element {
            position: absolute;
            background: var(--gold-light);
            border-radius: 50%;
            animation: floatAround 15s infinite linear;
        }

        @keyframes floatAround {
            0% { transform: translate(0, 0) rotate(0deg); }
            25% { transform: translate(20px, 40px) rotate(90deg); }
            50% { transform: translate(40px, 0) rotate(180deg); }
            75% { transform: translate(20px, -40px) rotate(270deg); }
            100% { transform: translate(0, 0) rotate(360deg); }
        }

        .loading-bar {
            height: 4px;
            background: var(--gold-gradient);
            position: fixed;
            top: 0;
            left: 0;
            width: 0%;
            z-index: 9999;
            transition: width 0.3s ease;
        }
    </style>
</head>
<body>
    <!-- شريط التحميل -->
    <div class="loading-bar" id="loadingBar"></div>

    <!-- رأس الصفحة -->
    <header id="mainHeader">
        <div class="nav-container">
            <div class="logo">
                <i class="fas fa-pen-fancy"></i>
                Graphos Services
            </div>
            <nav>
                <ul>
                    <li><a href="#home"><i class="fas fa-home"></i> الرئيسية</a></li>
                    <li><a href="#services"><i class="fas fa-concierge-bell"></i> الخدمات</a></li>
                    <li><a href="#cart"><i class="fas fa-shopping-cart"></i> السلة</a></li>
                    <li><button class="language-switcher" id="languageSwitcher">
                        <i class="fas fa-globe"></i> English
                    </button></li>
                </ul>
            </nav>
        </div>
    </header>

    <!-- قسم البطل -->
    <section class="hero" id="home">
        <div class="floating-elements">
            <div class="floating-element" style="width: 30px; height: 30px; top: 20%; left: 10%;"></div>
            <div class="floating-element" style="width: 50px; height: 50px; top: 60%; left: 80%;"></div>
            <div class="floating-element" style="width: 40px; height: 40px; top: 80%; left: 20%;"></div>
            <div class="floating-element" style="width: 25px; height: 25px; top: 30%; left: 70%;"></div>
        </div>
        <div class="hero-content">
            <div class="ai-badge">
                <i class="fas fa-robot"></i> مدعوم بالذكاء الاصطناعي
            </div>
            <h1>Graphos Services</h1>
            <p>نقدم لك مجموعة متكاملة من خدمات التصميم والإبداع بأسعار تنافسية وجودة عالية. من تصميم الشعارات إلى إنشاء المواقع الإلكترونية، نحن هنا لتحقيق رؤيتك باستخدام أحدث تقنيات الذكاء الاصطناعي.</p>
            <a href="#services" class="cta-button">اكتشف خدماتنا <i class="fas fa-arrow-down"></i></a>
        </div>
    </section>

    <!-- قسم الخدمات -->
    <section class="services" id="services">
        <div class="container">
            <div class="section-title">
                <h2>خدماتنا المميزة</h2>
            </div>
            <div class="services-grid" id="servicesGrid">
                <!-- خدمة تصميم الشعارات -->
                <div class="service-card">
                    <div class="service-icon">
                        <i class="fas fa-palette"></i>
                    </div>
                    <div class="service-content">
                        <h3><i class="fas fa-drafting-compass"></i> تصميم الشعارات</h3>
                        <table class="pricing-table">
                            <tr>
                                <th>مدة التسليم</th>
                                <th>السعر</th>
                                <th></th>
                            </tr>
                            <tr>
                                <td>4 أيام</td>
                                <td class="price">15 ريال</td>
                                <td><button class="add-to-cart" data-service="تصميم شعار" data-desc="تسليم خلال 4 أيام" data-price="15"><i class="fas fa-cart-plus"></i></button></td>
                            </tr>
                            <tr>
                                <td>3 أيام</td>
                                <td class="price">20 ريال</td>
                                <td><button class="add-to-cart" data-service="تصميم شعار" data-desc="تسليم خلال 3 أيام" data-price="20"><i class="fas fa-cart-plus"></i></button></td>
                            </tr>
                            <tr>
                                <td>يومين</td>
                                <td class="price">25 ريال</td>
                                <td><button class="add-to-cart" data-service="تصميم شعار" data-desc="تسليم خلال يومين" data-price="25"><i class="fas fa-cart-plus"></i></button></td>
                            </tr>
                            <tr>
                                <td>يوم واحد</td>
                                <td class="price">30 ريال</td>
                                <td><button class="add-to-cart" data-service="تصميم شعار" data-desc="تسليم خلال يوم واحد" data-price="30"><i class="fas fa-cart-plus"></i></button></td>
                            </tr>
                            <tr>
                                <td>6-7 ساعات</td>
                                <td class="price">40 ريال</td>
                                <td><button class="add-to-cart" data-service="تصميم شعار" data-desc="تسليم خلال 6-7 ساعات" data-price="40"><i class="fas fa-cart-plus"></i></button></td>
                            </tr>
                        </table>
                        <span class="negotiable">جميع الأسعار قابلة للتفاوض</span>
                    </div>
                </div>

                <!-- خدمة تصميم الفيديوهات -->
                <div class="service-card">
                    <div class="service-icon">
                        <i class="fas fa-video"></i>
                    </div>
                    <div class="service-content">
                        <h3><i class="fas fa-film"></i> تصميم الفيديوهات</h3>
                        <table class="pricing-table">
                            <tr>
                                <th>مدة الفيديو</th>
                                <th>السعر</th>
                                <th></th>
                            </tr>
                            <tr>
                                <td>30 ثانية</td>
                                <td class="price">20 ريال</td>
                                <td><button class="add-to-cart" data-service="تصميم فيديو" data-desc="مدة 30 ثانية" data-price="20"><i class="fas fa-cart-plus"></i></button></td>
                            </tr>
                            <tr>
                                <td>60 ثانية</td>
                                <td class="price">30 ريال</td>
                                <td><button class="add-to-cart" data-service="تصميم فيديو" data-desc="مدة 60 ثانية" data-price="30"><i class="fas fa-cart-plus"></i></button></td>
                            </tr>
                            <tr>
                                <td>90 ثانية</td>
                                <td class="price">40 ريال</td>
                                <td><button class="add-to-cart" data-service="تصميم فيديو" data-desc="مدة 90 ثانية" data-price="40"><i class="fas fa-cart-plus"></i></button></td>
                            </tr>
                            <tr>
                                <td>120 ثانية</td>
                                <td class="price">50 ريال</td>
                                <td><button class="add-to-cart" data-service="تصميم فيديو" data-desc="مدة 120 ثانية" data-price="50"><i class="fas fa-cart-plus"></i></button></td>
                            </tr>
                        </table>
                        <span class="negotiable">جميع الأسعار قابلة للتفاوض</span>
                    </div>
                </div>

                <!-- يمكنك إضافة المزيد من الخدمات هنا -->
                
            </div>
        </div>
    </section>

    <!-- بوت الذكاء الاصطناعي -->
    <div class="ai-assistant">
        <div class="ai-toggle" id="aiToggle">
            <i class="fas fa-robot"></i>
        </div>
        <div class="ai-chatbot" id="aiChatbot">
            <div class="chat-header">
                <h3><i class="fas fa-robot"></i> مساعد Graphos AI</h3>
                <button class="close-chat" id="closeChat"><i class="fas fa-times"></i></button>
            </div>
            <div class="chat-messages" id="chatMessages">
                <div class="message bot-message">
                    مرحباً! أنا مساعد Graphos AI. كيف يمكنني مساعدتك اليوم؟
                </div>
            </div>
            <div class="chat-input">
                <input type="text" id="chatInput" placeholder="اكتب رسالتك هنا...">
                <button class="send-button" id="sendMessage"><i class="fas fa-paper-plane"></i></button>
            </div>
        </div>
    </div>

    <!-- قسم السلة والدفع -->
    <section class="cart-section" id="cart">
        <div class="container">
            <div class="section-title">
                <h2>سلة المشتريات</h2>
            </div>
            <div class="cart-container">
                <div class="cart-items" id="cartItems">
                    <!-- سيتم عرض العناصر المضافة ديناميكيًا -->
                </div>
                <div class="cart-total">
                    الإجمالي: <span class="price" id="cartTotal">0 ريال</span>
                </div>
                <button class="checkout-button" id="checkoutButton">إتمام الشراء</button>
                <div class="payment-policy">
                    <h3><i class="fas fa-shield-alt"></i> سياسة الدفع والاسترجاع</h3>
                    <p>يتم الدفع مقدمًا عبر البطاقة البنكية قبل استلام الخدمة. جميع المعاملات آمنة ومشفرة. في حال عدم الرضا عن الخدمة، يمكنك طلب استرجاع المبلغ خلال 24 ساعة من استلام الخدمة.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- تذييل الصفحة -->
    <footer>
        <div class="container">
            <div class="footer-content">
                <div class="footer-section">
                    <h3>Graphos Services</h3>
                    <p>نقدم خدمات إبداعية متكاملة تلبي جميع احتياجاتك التصميمية باستخدام أحدث تقنيات الذكاء الاصطناعي.</p>
                    <div class="social-links">
                        <a href="#"><i class="fab fa-twitter"></i></a>
                        <a href="#"><i class="fab fa-instagram"></i></a>
                        <a href="#"><i class="fab fa-linkedin"></i></a>
                        <a href="#"><i class="fab fa-behance"></i></a>
                    </div>
                </div>
                <div class="footer-section">
                    <h3>اتصل بنا</h3>
                    <p><i class="fas fa-envelope"></i> info@graphos.com</p>
                    <p><i class="fas fa-phone"></i> +966 123 456 789</p>
                    <p><i class="fas fa-map-marker-alt"></i> الرياض، المملكة العربية السعودية</p>
                </div>
                <div class="footer-section">
                    <h3>مواعيد العمل</h3>
                    <p>السبت - الخميس: 9 ص - 6 م</p>
                    <p>الجمعة: مغلق</p>
                    <p>خدمة العملاء: متاحة 24/7</p>
                </div>
            </div>
            <div class="copyright">
                <p>جميع الحقوق محفوظة &copy; 2023 Graphos Services | تم التطوير باستخدام الذكاء الاصطناعي</p>
            </div>
        </div>
    </footer>

    <script>
        // كود JavaScript للتفاعلية والذكاء الاصطناعي
        document.addEventListener('DOMContentLoaded', function() {
            // عناصر DOM
            const loadingBar = document.getElementById('loadingBar');
            const mainHeader = document.getElementById('mainHeader');
            const languageSwitcher = document.getElementById('languageSwitcher');
            const servicesGrid = document.getElementById('servicesGrid');
            const aiToggle = document.getElementById('aiToggle');
            const aiChatbot = document.getElementById('aiChatbot');
            const closeChat = document.getElementById('closeChat');
            const chatMessages = document.getElementById('chatMessages');
            const chatInput = document.getElementById('chatInput');
            const sendMessage = document.getElementById('sendMessage');
            const cartItems = document.getElementById('cartItems');
            const
